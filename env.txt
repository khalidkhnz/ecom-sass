# Database URL
DATABASE_URL="postgresql://neondb_owner:npg_aLE9ODQZ8Ilm@ep-floral-sun-a559mfpf-pooler.us-east-2.aws.neon.tech/neondb?sslmode=require"

BETTER_AUTH_SECRET="secret"
BETTER_AUTH_URL=http://localhost:3000 #Base URL of your app

# Google OAuth
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# # Email (Nodemailer)
EMAIL_SERVER_HOST="smtp.gmail.com"
EMAIL_SERVER_PORT="465"
EMAIL_SERVER_USER="fundinbharat@gmail.com"
EMAIL_SERVER_PASSWORD="cbquucxmldqxhdwj"
EMAIL_SERVER_SECURE="true"
EMAIL_FROM="noreply@test.com" 


# Note: Replace the values above with your actual database credentials
# For production, use environment variables provided by your hosting platform
# For Neon, the URL format will be something like:
# postgres://username:password@endpoint.region.neon.tech/dbname?sslmode=require

# JWT Secret for authentication (if used)
# JWT_SECRET="your-jwt-secret" 